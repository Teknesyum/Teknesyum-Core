# chrisvoncsefalvay/claude-d3js-skill

- lisans yok · çıplak Agent Skill (plugin sarmalı yok) · ★229
- mekanizma: 0 kanca, 0 komut, 0 ajan, 1 skill, 0 MCP; 3 `references/*.md` + 3 `assets/` şablonu
- sıradan turda bağlama: ~478 bayt frontmatter description, ~120 token; SKILL.md 22,6 KB + references 48 KB yalnız tetiklenince. Ölçüm: frontmatter + `wc -c`
- premium: yok

## Ne yapar
d3.js ile bespoke görselleştirme yazmak için tek skill: ne zaman d3 ne zaman başka araç, ölçek referansı, renk şemaları, desen kataloğu ve iki JSX şablonu.

## Core'a alınacak
- **fikir**: SKILL.md'nin ilk bölümü "**Use d3.js for:** / **Consider alternatives for:**" — rafın kendi sınırını en başta yazması. Core raflarında "bu raf ne için değildir" satırı aynı işi görür ve yanlış rafın açılmasını keser.
- **fikir**: referansların üçe bölünmesi (ölçek / desen / renk), 22 KB'lik ana metnin yanında ayrı ayrı okunabilir dosyalar — Core'un raf içi bölümlemesine örnek.
- hiç (lisanssız; metni Core'a kopyalanamaz)

## Karar
Fikir notu — lisans yok, içerik Core'un alanı dışında; yalnız raf biçimi öğretici.
