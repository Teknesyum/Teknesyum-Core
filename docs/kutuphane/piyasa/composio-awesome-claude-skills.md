# ComposioHQ/awesome-claude-skills

- lisans yok (README Apache-2.0 rozeti taşıyor) · metin paketi (awesome listesi) + 1 eklenti · ★74657
- mekanizma: liste içeriği; deponun kendi eklentisi `connect-apps` (`claude --plugin-dir`, `/connect-apps:setup` komutu, Composio MCP ağ geçidine bağlanıyor). Kanca yok.
- sıradan turda bağlama: 0 — liste okunmadıkça hiçbir şey yüklenmiyor; `connect-apps` eklentisi kurulursa 1 komut + MCP ağ geçidi gelir.
- premium: var — liste, Composio MCP Gateway'in (1000+ entegrasyon, kimlik doğrulama, denetim günlüğü) reklam yüzeyi; ücretsiz anahtarla başlıyor.

## Ne yapar
1000'den fazla Claude Skill ve eklentiyi kategorilere ayıran derleme liste; yalnız Claude.ai ve Claude Code değil Codex, Cursor, Gemini CLI, Antigravity için de. Girişte "beceriler nasıl çalışılacağını söyler, MCP ağ geçidi eylem yeteneği verir" diyerek kendi ürününü konumlandırıyor.

## Core'a alınacak
- fikir: sonraki piyasa taramaları için aday havuzu — 1000+ girdilik kategorili liste, Core'un kütüphane raflarını beslerken kaynak olarak taranabilir.
- hiç: liste metninin kendisi raf değeri taşımıyor; kategori başlıkları dışında mekanizma anlatmıyor.

## Karar
Fikir notu — kurulacak bir şey yok ve satıcı yönlendirmesi ağır; yalnız aday havuzu kaynağı olarak kaydedilsin.
