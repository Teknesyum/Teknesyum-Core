# A/B sonucu — Core kurulu vs kurulu değil, taze oturumlar

**Tarih:** 2026-09-05. **Kollar:** eco (eklenti açık, profil eco) ve native-eco (eklenti yok), ikisi de
`claude -p`, sonnet/low, taze `CLAUDE_CONFIG_DIR`, taze klon; kullanıcının CLAUDE.md dosyası ikisinde de
yok (Windows'ta kimlik Credential Manager'da; koşular `CLAUDE_CODE_OAUTH_TOKEN` ile). Kabul betiği kör,
koşu bittikten sonra koşucu çağırır. Koşucu `bench/run.js`, ham sonuç `bench/ab-sonuc.jsonl`.

Eşik koşudan önce yazıldı: Core toplam ≤ native × 1,05 geçer; > × 1,10 hava cıva; arası kullanıcının.

## 1. tekrar (10 oturum, seed 7)

| görev | Core (eco) $ · süre · kabul | native $ · süre · kabul | Core/native | sözleşme/alt ajan |
|---|---|---|---|---|
| 01-slugify | 1.4577 · 1194 s · ✓ | 1.2310 · 808 s · ✓ | +18.4% | 0/0 |
| 02-click | 0.1830 · 62 s · ✓ | 0.2355 · 88 s · ✓ | -22.3% | 0/0 |
| 03-wrap-ansi | 0.1054 · 63 s · ✓ | 0.1057 · 54 s · ✓ | -0.3% | 0/0 |
| 04-gray-matter | 0.1368 · 64 s · ✓ | 0.1335 · 63 s · ✓ | +2.4% | 0/0 |
| 05-requests | 0.1518 · 60 s · ✓ | 0.1698 · 131 s · ✓ | -10.6% | 0/0 |
| **toplam** | **2.0347** | **1.8756** | **+8.5%** | |

Görev başına oran medyanı: **-0.3%**. Kabul 10/10.

**Hüküm (1. tekrar):** ARADA — kullanıcı karar verir.

Okuma: beş görevin hiçbirinde Core sözleşme açmadı, alt ajan göndermedi; T0 (sonnet/low) işi
kendisi yaptı. Eklentinin bu koşulardaki payı kancalar ve skill/rol tanımlarının system prompt'a
girmesi. Yani bu ölçüm relay'in bedeli değil, **eklentinin kurulu olmasının bedeli**; relay'in
bedeli tek oturumdaki katlama ölçümünde (ameliyat raporu). 01-slugify iki kolda da uzun
(13-20 dk, $1,2-1,5): yeşil alan görevi, spec'ten yazım; öbür dördü hata düzeltme, ~1 dk, $0,10-0,24.

Tek tekrar: gürültü payı bilinmiyor; 2. ve 3. tekrar aynı seed'le eklenir (`--repeat 3 --repeatFrom 2`).

## Görev 06 — relay tetikleyici (1. çift)

Dört dosyalı CLI görevi (`bench/gorevler/06-slugify-cli.md`), skill'in "iki ya da daha çok
dosya → bir sözleşme, bir builder" satırını tetiklemesi için yazıldı.

| kol | $ | süre | kabul | sözleşme / alt ajan | çıktı token | önbellek okuma | tur (yaklaşık) |
|---|---|---|---|---|---|---|---|
| Core (eco) | 0,5328 | 301 s | ✓ | **0 / 0** | 10.090 | 1.757.753 | ~55 |
| native | 0,2916 | 115 s | ✓ | 0 / 0 | 5.898 | 839.745 | ~33 |
| Core/native | **+82,7%** | 2,6× | | | 1,7× | 2,1× | 1,7× |

**Sözleşme açılmadı.** Çok dosyalı görevde de T0 işi kendisi yaptı; ama native'in iki katı
turda ve iki katı bedelle. Neden bilinmiyor: koşucu oturum transkriptini config diziniyle
birlikte siliyordu. Koşucu düzeltildi (`bench/oturumlar/<batch>/<görev>-<kol>-r<n>/` altına
`oturum.jsonl` + alt ajanlar kopyalanır); çift aynı seed'le 2. tekrar olarak yeniden koşuldu,
sonucu ve otopsisi aşağıda.

## 2. çift ve otopsi — ve A/B'yi geçersiz kılan bulgu

2. çift (transkriptli): Core $0,2877 · 90 s · ✓ · 0/0; native $0,3423 · 135 s · ✓ · 0/0.
Otopsi (`bench/ab-06-otopsi.md`): iki oturum da 13-16 araç çağrısı, aynı sırayla (package.json,
index.js, cli.js yaz, package.json, readme, test-cli.js, ava); **hiçbir relay/kanca/skill izi yok**;
ilk mesajın önbellek yazımı iki kolda birebir aynı (13.303 / 13.301 token).

Aynı system prompt, aynı önbellek: **eklenti "Core" kolunda yüklü değildi.** Koşucunun `eklentiKur`
adımı eklenti dizinini kopyalayıp `settings.json`'a `enabledPlugins` yazıyordu ama Claude Code'un
eklentiyi yüklemesi için gereken `plugins/installed_plugins.json` ve `plugins/known_marketplaces.json`
kayıtlarını yazmıyordu. Taze bir config dizininde haiku ile prob: relay skill'i ve worker ajanı **yok**;
kayıtlar eklenince **var** (`teknesyum-core:relay`, `teknesyum-core:worker`).

Sonuç: bu dosyadaki 1. tekrar ve görev 06'nın iki çifti **native'e karşı native** ölçtü. eco satırları
native'in tekrarı olarak geçerli, "Core" olarak geçersiz; +8,5 / +83 / −16 sayıları eklentiye ait değil,
aynı kolun kendi oynaklığı. (Bu, iki arm arasındaki "farkın" ne kadar gürültü olduğunu da gösteriyor:
aynı ayar, aynı görev, $0,29 - $0,53.) Koşucu düzeltildi; eklentili eco kolu bütün görevler için
yeniden koşuluyor (repeat 3), native satırları duruyor.

## Eklentili Core kolu (r3) — ilk gerçek A/B

Eklenti kaydı düzeltildikten sonra eco kolu altı görev için bir kez daha koştu (transkriptler
`bench/oturumlar/bmtoi6wkn-7/`; her birinde `teknesyum-core` izi var, eklenti yüklü). Native
satırları 01-05 için 1. tekrar, 06 için 2. çift.

| görev | Core (eklentili) $ · süre | native $ · süre | Core/native | sözleşme/alt ajan | ilk mesaj önbellek yazımı C/N |
|---|---|---|---|---|---|
| 01-slugify | 0,1440 · 37 s | 1,2310 · 808 s | −88,3% (geçersiz, aşağıda) | 0/0 | — |
| 02-click | 0,3057 · 108 s | 0,2355 · 88 s | +29,8% | 0/0 | 12.889 / 13.301 |
| 03-wrap-ansi | 0,1074 · 63 s | 0,1057 · 54 s | +1,6% | 0/0 | — |
| 04-gray-matter | 0,1366 · 60 s | 0,1335 · 63 s | +2,3% | 0/0 | — |
| 05-requests | 0,2521 · 66 s | 0,1698 · 131 s | +48,5% | 0/0 | 13.041 / 13.301 |
| 06-slugify-cli | 0,3984 · 124 s | 0,3423 · 135 s | +16,4% | **0/0** | 13.394 / 13.301 |
| **toplam, 01 hariç** | **1,2003** | **0,9868** | **+21,6%** | | |

Görev başına oran medyanı (01 hariç): +16,4%. Kabul 6/6.

**Hüküm (eşik):** +21,6 > 1,10 → **hava cıva** — ama "hava cıva" olan eklentinin bedeli değil,
tek koşuluk ölçümün kendisi; gerekçesi aşağıda.

**Görev 01 geçersiz.** Pinlenmiş klonda gerçek `index.js` zaten duruyor; görev "index.js'i yaz"
diyor. Eklentili koşu dosyayı okuyup testleri koşturdu ve 37 saniyede bitirdi ($0,14); native
koşu 13,5 dakika boyunca yeniden yazdı ($1,23). İki koşu aynı işi yapmadı. 01, A/B'de
kullanılamaz; düzeltmesi dondurma kuralı gereği yeni numarayla (klonda `index.js` silinerek).

**Eklentinin ölçülebilir sabit bedeli ~90 token/tur.** İlk mesajın önbellek yazımı (system prompt +
ilk kullanıcı mesajı) eklentiyle 12.889-13.394, eklentisiz 13.301: relay skill'inin adı+açıklaması
ve tek `worker` ajanı bundan ibaret. Tur başına %0,7'nin altında. Kancalar transkriptte iz
bırakmıyor (bu kolda hiçbiri konuşmadı; `guard` yalnız reddettiğinde konuşur).

**Farkın geri kalanı tur sayısı.** 02: önbellek okuma 851k'ya karşı 586k (26 asistan mesajı,
native ~20); 05: 840k'ya karşı 492k. Aynı system prompt, aynı görev, farklı yol. 03 ve 04'te
fark %2. Bu, native'in kendi tekrarları arasındaki oynaklıkla aynı büyüklükte (06 native r1/r2:
$0,29 / $0,34; eklentisiz "eco" r1/r2: $0,53 / $0,29).

**Relay hiç tetiklenmedi.** Dört dosyalı 06'da da T0 (sonnet/low) skill'i çağırmadı, sözleşme
açmadı, alt ajan göndermedi. Skill listede (`teknesyum-core:relay`, açıklaması "Use for any build,
fix, refactor, or new project") ama model onu seçmedi; kullanıcı CLAUDE.md'si de bu kolda yok
(orada "çok adımlı iş → relay" kuralı var). Yani A/B bugün **eklentinin varlığını** ölçüyor
(~90 token/tur, geri kalanı gürültü); **relay'in bedelini** ölçmek için T0'ın relay'i gerçekten
kullanması gerekiyor. Seçenekler: (a) Core koluna kullanıcının gerçek CLAUDE.md'sini vermek
(Core kullanıcısının gerçek ortamı bu), (b) skill açıklamasını tetikleyici yapmak (ürün değişikliği,
ölçüm için değil), (c) görev metnine "relay skill'ini kullan" yazmak (asimetri, reddedildi).
