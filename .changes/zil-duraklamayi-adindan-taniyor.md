bump: patch

The waiting bell now reads the name the notice carries instead of guessing from how long ago
something moved. A permission prompt, an agent needing an answer and an MCP dialog ring; an
idle reminder, a sign-in, a quota notice and a subagent finishing under a turn that goes on
do not. A build that sends no name falls back to the old reading.
