bump: minor

A new role, `clarifier`. A prompt the user starts with `??` opens it before any work begins:
the request is sharpened into an order and a prohibition, and the user's own sentence is
never rewritten, never replaced, and wins wherever the addition disagrees with it. The role
reads no files of its own; it is handed what the asker already gathered. It borrows the
advisor's rung rather than adding a row of its own, so nothing runs on an unmarked turn.
