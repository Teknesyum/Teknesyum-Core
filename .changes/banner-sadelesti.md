---
bump: minor
---

The banner drops the counts nobody acts on - the step tally, the file tally and the minutes
elapsed - and stops shouting: only names are capitalised now, not every word. A `??` turn is
marked on the banner while the request is being sharpened, and the file line says who is
being consulted instead of a bare `last:`.
