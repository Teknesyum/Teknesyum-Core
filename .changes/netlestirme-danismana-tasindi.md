---
bump: minor
---

The `??` mark opens the advisor itself instead of a role of its own. A marked turn changes
two things only: the user's sentence becomes the first line of the input, and the return is
a question, a fact and a route instead of a call. `clarifier.md` moved to `trash/`.
