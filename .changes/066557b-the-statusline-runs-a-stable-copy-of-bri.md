bump: minor

The statusline runs a stable copy of bridge.js under ~/.claude/teknesyum/, so pruning old plugin versions can no longer break it; doctor gains a cache check that names every repo file the installed plugin does not run yet.
