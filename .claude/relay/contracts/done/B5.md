---
result: passed
id: B5
status: done
round: 1
lang: tr
role: builder
tier: sonnet/high
owns: [bench/run.js, test/bench-onkontrol.js]
verify:
  - node test/bench-onkontrol.js
  - node bench/run.js --plan --scope pilot
---

## Goal
Koşucuya bir ön kontrol adımı ekle: koşular başlamadan önce kimlik doğrulamanın çalıştığını kanıtla, çalışmıyorsa hiçbir koşu başlatmadan net bir mesajla dur.

Gerekçe ölçüldü: temiz bir `CLAUDE_CONFIG_DIR` ile `claude -p` **"Not logged in · Please run /login"** deyip 1 dönüyor. Bu hâliyle 36 koşunun 36'sı da başarısız olur, `sonuc.jsonl` çöple dolar ve saatler boşa gider.

## Acceptance
- `node bench/run.js --onkontrol` hiçbir görev klonlamadan tek bir ucuz çağrı ile kimlik doğrulamayı sınar: koşuların kullanacağı yolun aynısıyla geçici bir config dizini hazırlar, `claude -p` ile kısa bir istem gönderir, dönen kodu ve çıktıyı raporlar. Başarılıysa 0, değilse 1 döner.
- Gerçek koşu (`--plan` ve `--onkontrol` olmadan) ilk görevi klonlamadan **önce** aynı ön kontrolü çalıştırır. Başarısızsa hiçbir koşu başlatılmaz, `bench/sonuc.jsonl` dosyasına tek satır yazılmaz ve süreç 1 ile durur.
- Ön kontrol başarısızken basılan mesaj ne yapılacağını söyler: kimlik doğrulamanın koşu başına temiz config dizininde bulunmadığını, ve `ANTHROPIC_API_KEY` ortam değişkeni ayarlanarak ya da `claude` ile bir kez giriş yapılmış bir config dizini `--configTemplate <dizin>` ile verilerek çözüleceğini. Suçlama değil, yönerge.
- `--configTemplate <dizin>` bayrağı eklenir: verildiğinde her koşu o dizini kendi geçici config dizinine kopyalayarak başlar. Kopyalama **izin listesiyle** çalışır: yalnız adı listede geçen girdiler geçer, listede olmayan her şey atlanır. Yasak listesi kullanılmaz — gelecekte config dizinine eklenecek yeni bir yönerge ya da önbellek dosyası sessizce sızmamalı. Liste kodda tek yerde durur, dışa aktarılır ve testten görülür; `docs/BENCH.md` bölüm 4 temizlik güvencesi buna dayanır.
- `ANTHROPIC_API_KEY` ortamda tanımlıysa ön kontrol ve koşular onu kullanır; bu durumda `--configTemplate` gerekmez.
- `test/bench-onkontrol.js` sahte bir çalıştırıcı ile dört durumu doğrular: ön kontrol geçince koşuların başladığı, geçmeyince hiçbir koşunun başlamadığı ve `sonuc.jsonl`a yazılmadığı, `--configTemplate` ile kopyalanan dosya listesinin yasaklı adları içermediği, ve `ANTHROPIC_API_KEY` varken şablon istenmediği.
- Mevcut `test/bench-kosu.js` ve `test/bench-maliyet.js` yeşil kalır; ikisi de `owns` dışında, onlara dokunma.

## Checkpoint

1. tur teslim edildi; ön kontrol gerçek makinede sınandı ve doğru davrandı — `node bench/run.js --onkontrol` kod 1 ile durdu, mesaj iki çözümü de söyledi, hiçbir koşu başlamadı. Dört sav ve iki eski test yeşil. Tek düzeltme: kopyalama yasak listesiyle değil izin listesiyle yapılacak.

2. tur: `YASAKLI_CONFIG_GIRDILERI` (kara liste) kaldırıldı, yerine `IZIN_VERILEN_CONFIG_GIRDILERI`
(beyaz liste) — `['.credentials.json', '.claude.json']`, dar ve ölçülmemiş (bu makinede giriş
yapılmış bir config dizini yok), tek yerde tanımlı ve dışa aktarılıyor. `configSablonundanKopyala`
artık yalnız bu iki adı kopyalar; listede olmayan her şeyi (bilinen kirletici ya da uydurma
dosya) sessizce atlar, eksik girdi hata üretmez. Test güncellendi: izin listesi dışındaki bir
dosyanın (`sizinti-denemesi.md`) kopyalanmadığını ve boş şablonda hiç hata atılmadığını
doğruluyor. 5/5 yeni test, 8/8 ve 9/9 eski testler yeşil.
