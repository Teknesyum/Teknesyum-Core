---
result: passed
id: B2
status: done
round: 1
lang: tr
role: builder
tier: sonnet/high
owns: [bench/gorevler/01-slugify.md, bench/gorevler/01-slugify.kabul.sh, bench/gorevler/02-click.md, bench/gorevler/02-click.kabul.sh, bench/gorevler/README.md]
verify:
  - bash -c 'test -s bench/gorevler/01-slugify.md && test -s bench/gorevler/01-slugify.kabul.sh && test -s bench/gorevler/02-click.md && test -s bench/gorevler/02-click.kabul.sh'
  - bash -n bench/gorevler/01-slugify.kabul.sh
  - bash -n bench/gorevler/02-click.kabul.sh
---

## Goal
Pilotun iki görevini yaz ve dondur: `sindresorhus/slugify` (yeşil alan benzeri, JS) ve `pallets/click` (hata düzeltme, Python).

Şartname `docs/BENCH.md`, özellikle bölüm 3 ve 4.

## Acceptance
- Her görev için `NN-<ad>.md` görev metnini, `NN-<ad>.kabul.sh` kabul testini taşır. Görev metni **tek prompt** olarak verilecek biçimde yazılır; ajan ile arkasından konuşulmayacağı varsayılır.
- Görev metni harness'tan bağımsızdır: sözleşme, rele, `owns`, `verify` gibi Core'a özgü hiçbir terim geçmez. Aynı metin native kola da verilebilmelidir.
- Her görev pinlenmiş bir commit sha'sı taşır; dal adı değil sha yazılır ve `README.md` içinde sha'nın hangi tarihte alındığı kaydedilir.
- Kabul testi kol etiketi görmez: yalnız çalışma dizinini alır, geçti/kaldı için 0 ya da 1 döner, kararını stdout'a tek satır yazar.
- Kabul testi ölçtüğü şeyi gerçekten ölçer — `slugify` için Unicode kenar durumlarını kapsayan somut savlar, `click` için depodaki reprodüksiyon testine dayanan somut bir hata. İkisi de deponun mevcut takımını çalıştırıp yeşil olmasını da şart koşar.
- Görevlerin zorluğu 30 dakikalık duvar tavanının altında bitebilecek ölçüdedir; `README.md` her görev için tahmini süreyi ve zorluk gerekçesini yazar.
- `README.md` dondurma tarihini taşır ve "bu dosyalar koşu başladıktan sonra değiştirilmez" kuralını yazar.

## Checkpoint

1. tur teslim edildi. Görev metinleri, pinler, referans doğrulama (gerçek çözümle PASS, çözümsüz FAIL) ve README sağlam çıktı; dışlanan click testinin gerekçesi de kanıtlı. T0 denetimi iki kusur buldu: kabul testleri ortamdaki Python kurulumunu değiştiriyor (her koşuda `pip install -e .` ve `pip install pytest==9.0.2`, sanal ortam yok) ve sabit `/tmp` dosya adları kullanıyor. Ayrıca `owns` dışında iki dosya oluşmuştu (`sahte-gorev.md`, `sahte-gorev.kabul.sh`); T0 ikisini `trash/` altına taşıdı, `_issues.log`a yazıldı. 2. tur açıldı.

2. tur: `02-click.kabul.sh` artık `mktemp -d` ile geçici bir dizinde kendi `venv`'ini
kuruyor (`python -m venv`), `click`'i ve `pytest==9.0.2`'yi yalnız o venv'e kuruyor,
testleri o venv'in yorumlayıcısıyla koşuyor, biterken `trap`'siz ama her çıkış yolunda
(`fail()` ve normal biten) çağrılan `cleanup()` ile venv'i ve `mktemp` ile üretilmiş
geçici log/script dosyalarını siliyor. Ana makinenin `python`/`pip`'ine dokunmuyor.
Referans doğrulama izole venv ile tekrarlandı: bug'lı pin (git stash ile düzeltme geri
alınarak) **FAIL**, gerçek PR düzeltmesi geri uygulanınca **PASS** — ikisinde de ana
Python'da `click`/`pytest` kurulu kalmadı, geçici dizin koşu sonunda yoktu. README
dondurma tarihi 2026-09-05T00:10:28Z'ye güncellendi, kanıt paragrafları yenilendi.

Ek madde: her iki görev `.md` dosyasının başına `repo:`/`sha:` frontmatter'ı eklendi
(koordinatörün isteği — B1'in koşucusu pini README'den ayrıştırmaya çalışmış, artık
`gorevOku` frontmatter okuyor). Gövdedeki sha bahsi dokunulmadan kaldı, tek-prompt
metin kendi başına tutarlı. README'ye pinin işlevsel kaynağının frontmatter olduğunu
belirten tek cümle eklendi, pinlerin nasıl alındığına dair mevcut metne dokunulmadı.

## Acceptance — 2. tur ek maddeleri

- **Kabul testi makinenin Python kurulumunu değiştirmez.** `02-click.kabul.sh` kendi sanal ortamını çalışma dizininin dışında, geçici bir yerde kurar (`python -m venv`), `click` ile `pytest==9.0.2` oraya kurulur, testler o ortamın yorumlayıcısıyla koşulur ve betik biterken ortam silinir. Şu anki hâli 18 click koşusunun her birinde ana yorumlayıcıyı kirletiyor; `docs/BENCH.md` bölüm 4 her koşunun temiz olmasını şart koşuyor ve bu ona aykırı.
- **Geçici dosya adları koşuya özgüdür.** `/tmp/click-kabul-suite.log`, `/tmp/click-kabul-repro.py`, `/tmp/click-kabul-repro.log` gibi sabit adlar yerine `mktemp` ile üretilmiş adlar kullanılır ve betik biterken silinir. Koşucu şu an sıralı çalışıyor ama sabit ad, ileride paralel koşuya geçildiğinde sessizce yanlış sonuç üretir.
- **Her iki kabul testi de `owns` dışına dosya bırakmaz.** Fikstür, günlük ve ara dosya depo ağacına yazılmaz.
- Değişiklikten sonra referans doğrulama tekrarlanır: pinlenmiş commit + gerçek düzeltme → PASS, düzeltmesiz pin → FAIL. Sonuç `README.md` içinde güncellenir.