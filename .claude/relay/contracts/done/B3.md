---
result: passed
id: B3
status: done
round: 1
lang: tr
role: builder
tier: sonnet/high
owns: [bench/rapor.js, test/bench-rapor.js]
verify:
  - node test/bench-rapor.js
---

## Goal
`bench/sonuc.jsonl` satırlarından `bench/rapor.md` tablosunu üret.

Şartname `docs/BENCH.md`, özellikle bölüm 0, 1 ve 5. Satır şeması bölüm 5'te yazılı; `bench/run.js` henüz yazılıyor, ona bakma, şartnameye göre yaz.

## Acceptance
- `node bench/rapor.js` `bench/sonuc.jsonl` okuyup `bench/rapor.md` yazar; tablo `docs/BENCH.md` bölüm 5'teki altı satırlık çift/kol düzenindedir.
- Her hücre **medyan + min–max** taşır, ortalama değil. Tek koşuluk bir hücre rakam yerine `tek koşu — raporlanmaz` yazar.
- Rapor tarih, model id ve Claude Code sürümünü taşır; bir kol içinde birden fazla `modelId` ya da `ccVersion` görülürse tabloyu üretmez, hangi kolda hangi iki değerin çakıştığını yazıp 1 döner.
- Durma kuralı `docs/BENCH.md` bölüm 0daki iki basamaklı biçimde değerlendirilir ve her basamakta **medyan** kullanılır. Bir görev, Core kolunun o görevdeki medyan $ değeri eşlendiği native kolunun medyan $ değerini %40tan fazla aşıyorsa tutar. Koşular indeksle birbirine eşleştirilmez — Core 2. tekrar ile native 2. tekrar bağımsız koşulardır. Bayrak, çift **en az iki** görevde tutuyorsa kalkar; `every` ile bütün görevlerin tutması aranmaz. Medyanı hesaplanamayan hücre (tek koşu, ya da eşlenmiş native koşusu yok) tutmaz sayılır ve raporda belirtilir. Sonuç raporun başına tek satır olarak yazılır.
- `test/bench-rapor.js` en az beş durumu doğrular: medyan ve min–max doğru hesaplanıyor; karışık `modelId` tabloyu reddediyor; bayrak tek görevde tutan kümede kalkmıyor ama iki görevde tutanda kalkıyor; **beş görevin ikisinde tutan bir kümede de kalkıyor** (yani `every` değil, en az iki); ve Core kolunda tek bir aykırı pahalı koşu varken medyan sağlam kaldığı için bayrak kalkmıyor.

## Checkpoint

1. tur teslim edildi, testler yeşildi. T0 denetiminde iki kusur çıktı: durma kuralı medyan yerine koşu-koşu karşılaştırma yapıyordu (`evaluateStopRule` içinde `repeat` indeksiyle eşleştirme), ve `taskIds.every` tam kapsamda beş görevin beşini birden şart koşuyordu. `docs/BENCH.md` bölüm 0 buna göre netleştirildi. 2. tur açıldı.

2. tur: `evaluateStopRule` yeniden yazıldı. Artık görev bazında Core ve native
kolunun o görevdeki tüm tekrarlarının **medyan $**'ı hesaplanıp karşılaştırılıyor
(koşu-koşu/`repeat` eşleştirmesi kaldırıldı). Bayrak eşiği `every` yerine
`holdCount >= 2` (en az iki görev). Medyanı hesaplanamayan görev (tek koşu ya
da eşlenmiş native koşusu yok) `taskUnresolved` ile işaretlenip tutmaz sayılıyor
ve `stopRuleLine` bunu rapora not düşüyor. `test/bench-rapor.js` 6 durumu
doğruluyor (istenen 5 + medyan/min-max): tek görevde tutmuyor, iki görevde
tutuyor, beş görevin ikisinde tutuyor (every değil), tek aykırı pahalı koşu
medyanı bozmuyor. `node test/bench-rapor.js` yeşil.

Denetim: tek koşuluk hücre kusuru bulundu — `coreUsd.length === 0` yalnız hiç
koşu yokluğunu çözümsüz sayıyordu, tek koşu (n=1) medyanı yine hesaplayıp
bayrağı kaldırabiliyordu. `coreUsd.length < 2 || nativeUsd.length < 2` olarak
düzeltildi — Core ya da native tarafında tek koşu varsa görev `taskUnresolved`
olur, `taskHolds` false kalır. İki teste eklendi: Core'da tek koşu native
medyanının iki katı olsa da tutmuyor; native'de tek koşu varken de tutmuyor.
`node test/bench-rapor.js` yeşil (8 durum). Düzeltme uygulandı, teslime hazır.
