---
result: passed
id: B1
status: done
round: 1
lang: tr
role: builder
tier: sonnet/high
owns: [bench/run.js, bench/maliyet.js, test/bench-maliyet.js, test/bench-kosu.js]
verify:
  - node test/bench-maliyet.js
  - node test/bench-kosu.js
  - node bench/run.js --plan --scope pilot
---

## Goal
Bench koşucusunu ve maliyet toplayıcısını yaz: koşuları sürer, her koşuyu `bench/sonuc.jsonl` dosyasına ekler, maliyeti transcript'ten dört kalem ayrı hesaplar.

Şartname `docs/BENCH.md`, özellikle bölüm 1, 2, 4 ve 5. Şartnameyi okumadan başlama.

## Acceptance
- `node bench/run.js --plan --scope pilot` hiçbir koşu başlatmadan planı basar ve 0 döner: 2 görev × 3 tekrar × 6 kol = 36 satır, her satırda `taskId`, `arm`, `seat`, `repeat`. `--scope tam` aynı biçimde 90 satır basar.
- Kollar `docs/BENCH.md` bölüm 2'deki altı koldur ve koltukları `core/tiers.json`'daki builder hücresinden okunur, dosyaya elle yazılmaz.
- Koşu sırası karışıktır, kol kol blok değil; sıra `--seed <n>` ile yeniden üretilebilir.
- Her koşu kendi geçici `CLAUDE_CONFIG_DIR` dizininde açılır ve koşu bitince o dizin silinir. Core kolları için eklenti kurulumu betiğin içinde yapılır; native kolları için config dizini boş bırakılır.
- Koşu başına 30 dakika duvar tavanı vardır; tavana çarpan koşu `dropped: true`, `dropReason: "ceiling"` ile kaydedilir ve süreç ağacı öldürülür.
- `bench/maliyet.js` bir transcript dizininden dört kalemi ayrı ayrı toplar: `input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`. Toplama ana oturum jsonl'i **ve** `<oturum>/subagents/agent-*.jsonl` altındaki her alt ajan girer, model başına ayrılır.
- Dolar tutarı `docs/tarife.json` dosyasından hesaplanır; dosya yoksa, modelin satırı yoksa ya da dört kalemden biri eksikse koşu hata verip durur, sıfır varsaymaz. `docs/COST-MODEL.md` fiyat tablosu değildir, oraya bakma.
- `bench/sonuc.jsonl` satırı `docs/BENCH.md` bölüm 5'teki alanların hepsini taşır. Dosya her zaman eklenerek yazılır, hiçbir koşu önceki satırları silmez veya yeniden yazmaz.
- `test/bench-maliyet.js` sahte bir transcript ağacı kurar (ana + iki alt ajan, farklı modeller) ve `maliyet.js`in dört kalemi de model başına doğru topladığını, alt ajanları atlamadığını doğrular. Aynı `message.id` iki satırda göründüğünde bir kez sayıldığını da doğrular.

2. tur maddeleri:

- **Koltuğun effort yarısı kaybolmaz.** `sonnet/low`, `sonnet/medium`, `sonnet/high` üç ayrı koltuktur; `claude` çağrısı model **ve** effort taşır. Effort taşınmadan üç çift birbirinin aynısı olur ve `eşle` kararı anlamını yitirir.
- **Core kolunun profili gerçekten kurulur.** `eco`, `normal`, `premium` birbirinden ancak `~/.claude/teknesyum/config.json` içindeki `profile` alanıyla ayrılır; geçici `CLAUDE_CONFIG_DIR` içine bu dosya yazılmadan üç Core kolu da varsayılan profille koşar. Kurulum adımı bu dosyayı kolun profiliyle yazar, ve `lang`/`contractLang` gibi zorunlu alanları da doldurur.
- **`modelId` gerçek model kimliğidir**, koltuk adının yarısı değil: `claude-sonnet-5` gibi. Kaydedilen değer transcript'ten okunur; okunamazsa satır `modelId: null` taşır ve `dropReason` bunu söyler. `sonnet` yazmak B3'ün karışık-model kontrolünü işlevsiz bırakır.
- **`ccVersion` Claude Code sürümüdür**, deponun `package.json` sürümü değil. `claude --version` çıktısından okunur, koşu başlamadan bir kez.
- **Altyapı hatası başarısızlıkla karıştırılmaz.** `git clone` ya da `git checkout` sıfırdan farklı dönerse koşu `dropped: true`, `dropReason: "setup"` ile kaydedilir; kabul testi hiç çalıştırılmaz ve `pass: false` bir ölçüm sonucu gibi raporlanmaz.
- **Tavana çarpan koşunun harcaması da kaydedilir.** `dropped: true` olsa bile transcript varsa `tokens`, `usd` ve `usdSource` doldurulur; o para harcandı, rapordan düşmez.
- `test/bench-kosu.js` gerçek `claude` çağırmadan koşu yolunu doğrular: sahte bir çalıştırıcı enjekte edilir ve (a) her kol için geçen argümanlar ile `CLAUDE_CONFIG_DIR` beklendiği gibi mi, (b) Core kollarında `config.json` doğru profille yazılıyor mu, (c) üç Core kolu birbirinden farklı effort ve profil ile mi çağrılıyor, (d) `setup` ve `ceiling` düşüşleri doğru `dropReason` ile mi kaydediliyor — dördü de savla doğrulanır.

## Checkpoint

1. tur teslim edildi; `maliyet.js` ve testi sağlam çıktı — aynı `message.id` tekrarının bir kez sayılması gerçek transcript'e karşı doğrulandı ve doğruydu. T0 denetimi `run.js` tarafında dört kusur buldu: koltuğun effort yarısı `claude` çağrısına hiç geçmiyor, Core kolunun profili için `config.json` yazılmıyor (üç Core kolu da varsayılan profille koşardı), `modelId` alanına `sonnet` gibi koltuk yarısı yazılıyor, `ccVersion` Claude Code yerine deponun sürümünü alıyor. Ayrıca altyapı hatası ile ölçüm başarısızlığı ayrılmıyor ve tavana çarpan koşunun harcaması kaydedilmiyor. 2. tur açıldı.

**Engel:** `verify` listesine `node test/bench-kosu.js` eklenmiş ama `owns` bu dosyayı taşımıyor (`owns: [bench/run.js, bench/maliyet.js, test/bench-maliyet.js]`). Dosyayı yazmaya çalıştım, guard.js engelledi: "test/bench-kosu.js is outside the owns set of B1." `owns`'a `test/bench-kosu.js` eklenmeden 2. tur kapanamaz.

Bu arada `run.js` içinde 2. tur maddelerinin hepsi uygulandı (aşağıda), yalnız `test/bench-kosu.js` yazılamadığı için `verify` listesindeki o adım koşulamıyor:
- `--model <model> --effort <effort>` ikisi de `claude` çağrısına geçiyor (CLI'da gerçek bayrak: `claude --help` çıktısında doğrulandı, `--effort <level>` ayrı bayrak).
- Core kollarında `eklentiKur(configDizini, profil)` artık `<configDizini>/teknesyum/config.json` dosyasını `profile`, `lang`, `contractLang` ve diğer zorunlu alanlarla yazıyor; native kollarında hiç çağrılmıyor.
- `modelId` artık `maliyet.anaModel(oturumDizini)` ile transcript'ten okunuyor (yeni export, `bench/maliyet.js`'e eklendi, `test/bench-maliyet.js`'e iki durum eklendi). Okunamazsa `dropped:true`, `dropReason:'modelId-okunamadi'`.
- `ccVersion` artık `claude --version` çıktısından bir kez okunuyor (`ccVersionOku`), tüm satırlara aynı değer yazılıyor.
- `git clone`/`checkout` başarısız dönerse kabul testi hiç çalışmadan `dropped:true`, `dropReason:'setup'` ile kayıt düşülüp döner.
- Tavana çarpan koşuda (`dropReason:'ceiling'`) transcript bulunursa `tokens`/`usd`/`usdSource` yine dolduruluyor, boş bırakılmıyor.
- `koşuYap` artık `git`/`calistirici`/`kabulCalistir`/`mkdtemp` bağımlılıklarını enjekte edilebilir yapıyor (gerçek `claude` çağırmadan test edilebilsin diye) ve `bench/run.js` bunları named export olarak dışa veriyor.

`node bench/run.js --plan --scope pilot` → 36 satır, 0. `node bench/run.js --plan --scope tam` → 90 satır, 0. `node test/bench-maliyet.js` → 9/9 PASS (iki yeni `anaModel` durumu eklendi).

`test/bench-kosu.js`'in taslağı hazır (altı durumu kapsıyor: argüman/`CLAUDE_CONFIG_DIR` eşleşmesi, core/native `config.json` farkı, üç Core kolunun farklı effort+profille çağrılması, `setup`/`ceiling` `dropReason`'ları, gerçek `modelId`'nin transcript'ten okunması) ama `owns` genişletilmeden diske yazılamıyor. Testi repo dışında (scratchpad) çalıştırıp `bench/run.js`'in `koşuYap` export'una karşı doğruladım, altısı da PASS — `owns` açılınca dosyayı olduğu gibi `test/bench-kosu.js`'e taşımak yeterli.

Ayrıca B2'nin gerçek görev dosyaları gelince fark ettim: `bench/gorevler/01-slugify.md` ve `02-click.md` frontmatter taşımıyor, `repo`/`sha` `README.md`'deki `## NN — <repo>` başlığı ve `**Pinlenen commit:** \`<sha>\`` satırından geliyor. `gorevOku`'yu buna göre düzelttim (`gorevMetaOku` README'yi ayrıştırıyor); gerçek `01-slugify`/`02-click` dosyalarına karşı `repoPin` doğru üretildiğini doğruladım (`https://github.com/sindresorhus/slugify.git@2acf5...`, `https://github.com/pallets/click.git@a5f5aa6...`).

Doğrulama (owns içindeki iki adım): `node test/bench-maliyet.js` → 0 (9/9 PASS). `node bench/run.js --plan --scope pilot` → 0 (36 satır). `node test/bench-kosu.js` çalıştırılamıyor çünkü dosya `owns` dışında.

**Engel kapatıldı, iki düzeltme daha:**

1. `owns`'a `test/bench-kosu.js` eklendi. Scratchpad'deki taslağı olduğu gibi `test/bench-kosu.js`'e taşıdım; fikstürü (`sahte-gorev.md`, `bozuk-gorev.md`) `fs.mkdtempSync` ile geçici bir dizinde kuruyor, `koşuYap`'a yeni `bagimlar.gorevKok` alanıyla enjekte ediyor, testler bitince o geçici dizini siliyor — depo ağacına hiçbir dosya bırakmıyor.
2. `gorevOku`'yu README ayrıştırmasından geri aldım, 1. turdaki gibi görev dosyasının kendi `repo:`/`sha:` frontmatter'ını okuyor. Frontmatter yoksa ya da `repo`/`sha` eksikse `Error('gorev frontmatter eksik: ...')` ile net durur, README'ye ya da başka bir kaynağa sessizce düşmüyor — bu davranış `test/bench-kosu.js`'te ayrı bir durumla doğrulandı. B2'nin `01-slugify.md`/`02-click.md` dosyaları zaten frontmatter'lı geldiği için `gorevOku` gerçek dosyalarla da doğrulandı.

`test/bench-kosu.js` sekiz durumu kapsıyor: argüman/`CLAUDE_CONFIG_DIR` eşleşmesi, core kolunda `config.json` profili, native kolunda `config.json` yokluğu, üç Core kolunun farklı effort+profille çağrılması, `setup` düşüşü (kabul hiç çalışmıyor), `ceiling` düşüşünde harcamanın kaydedilmesi, gerçek `modelId`'nin transcript'ten okunması, frontmatter eksikse net hata.

Üç `verify` adımının üçü de yeşil: `node test/bench-maliyet.js` → 0 (9/9 PASS), `node test/bench-kosu.js` → 0 (8/8 PASS), `node bench/run.js --plan --scope pilot` → 0 (36 satır). `git status --porcelain` fikstür/artık dosya bırakmadığını doğruluyor.
