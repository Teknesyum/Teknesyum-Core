---
result: passed
id: B4
status: done
round: 1
lang: tr
role: builder
tier: sonnet/high
owns: [bench/gorevler/03-wrap-ansi.md, bench/gorevler/03-wrap-ansi.kabul.sh, bench/gorevler/04-gray-matter.md, bench/gorevler/04-gray-matter.kabul.sh, bench/gorevler/05-requests.md, bench/gorevler/05-requests.kabul.sh, bench/gorevler/02-click.kabul.sh, bench/gorevler/README.md]
verify:
  - node -e "const fs=require('fs'),cp=require('child_process');for(const f of ['03-wrap-ansi','04-gray-matter','05-requests']){for(const e of ['.md','.kabul.sh']){const q='bench/gorevler/'+f+e;if(!fs.statSync(q).size)throw new Error('bos: '+q)}if(cp.spawnSync('bash',['-n','bench/gorevler/'+f+'.kabul.sh']).status!==0)throw new Error('sozdizimi: '+f)}"
  - bash -n bench/gorevler/02-click.kabul.sh
  - node -e "const fs=require('fs');for(const f of ['03-wrap-ansi','04-gray-matter','05-requests']){const s=fs.readFileSync('bench/gorevler/'+f+'.md','utf8');const m=s.match(/^---\r?\n([\s\S]*?)\r?\n---/);if(!m)throw new Error('frontmatter yok: '+f);if(!/repo:/.test(m[1])||!/sha:/.test(m[1]))throw new Error('repo/sha eksik: '+f);if(!/^[0-9a-f]{40}$/m.test(m[1].match(/sha:\s*(\S+)/)[1]))throw new Error('sha 40 hane degil: '+f)}"
---

## Goal
Tam kapsamın kalan üç görevini yaz ve dondur: `chalk/wrap-ansi` (refactor, JS), `jonschlinkert/gray-matter` (hata düzeltme, JS), `psf/requests` tek modül (refactor, Python). Ayrıca 02'de kalan geçici dizin sızıntısını kapat.

Şartname `docs/BENCH.md` bölüm 3 ve 4. Biçim ve ölçüt olarak `01-slugify` ile `02-click` örnektir — onlara bak, aynı düzeni sürdür.

## Acceptance
- Üç görev de `NN-<ad>.md` + `NN-<ad>.kabul.sh` çiftini taşır. `.md` dosyası `repo:` ve `sha:` frontmatter'ı ile başlar; `sha` 40 haneli gerçek bir commit'tir, `git ls-remote` ya da `gh api` ile alınır ve alındığı komut `README.md`'ye yazılır. Dal adı yazılmaz.
- Frontmatter'ın altındaki gövde ajana **tek prompt** olarak verilir ve kendi başına anlaşılırdır; harness'a özgü hiçbir terim (sözleşme, rele, owns, verify) geçmez, aynı metin native kola da verilebilir.
- Kabul testi yalnız çalışma dizinini alır, kol etiketi görmez, 0/1 döner ve kararını tek satır `PASS:`/`FAIL:` ile yazar.
- Kabul testi ölçtüğü şeyi gerçekten ölçer: **refactor görevlerinde** (03, 05) deponun mevcut takımı yeşil kalmalı **ve** davranışın değişmediği ayrı savlarla gösterilmeli; **hata düzeltme görevinde** (04) depodaki takıma ek olarak hatanın reprodüksiyonu düzelmiş olmalı. Testleri ezberleyip geçen sahte bir çözüm yakalanmalı.
- Her kabul testi ortamı kirletmez: Python tarafı kendi `venv`ini geçici yerde kurar, Node tarafı kurulumunu çalışma dizininde tutar, bütün geçici dosyalar `mktemp` ile üretilir ve çıkışta silinir.
- `02-click.kabul.sh` içindeki sızıntı kapatılır: `VENV="$(mktemp -d)/click-kabul-venv"` deseni `mktemp -d` dizininin kendisini de temizleyecek biçimde düzeltilir, ve `cleanup` bir `trap` ile sinyal/öldürme durumunda da çalışır. Yeni üç betik de aynı `trap` desenini kullanır.
- Her görev için referans doğrulama yapılır ve `README.md`'ye yazılır: pinlenmiş commit + gerçek çözüm → PASS, çözümsüz/bozuk hâl → FAIL.
- `README.md` üç yeni görevin pinini, tahmini süresini, zorluk gerekçesini ve dondurma tarihini taşır. Her görev 30 dakikalık duvar tavanının altında bitebilecek ölçüde olmalı; olmuyorsa kapsam daraltılır ve gerekçesi yazılır.
- `owns` dışına tek dosya bırakılmaz — fikstür, günlük, ara dosya hiçbiri depo ağacına yazılmaz.

## Checkpoint
Açıldı.

Üç alt ajan paralel çalıştırıldı (wrap-ansi, gray-matter, requests) — her biri kendi
depo pinini `git ls-remote`/`gh api` ile aldı, scratchpad'de klonlayıp gerçek referans
doğrulaması (PASS/FAIL) yaptı. `02-click.kabul.sh` sızıntısı (`mktemp -d` üst dizini
temizlenmiyordu) `VENV_ROOT` ayrıştırması ve `trap cleanup EXIT HUP INT TERM` ile
kapatıldı, gerçek klonla tekrar `PASS` alındı. `05-requests.kabul.sh`'ta aynı sızıntı
ve eksik `trap` tekrarlanmıştı, aynı desenle düzeltildi ve gerçek klonla `PASS`
doğrulandı. `03-wrap-ansi.md`'de frontmatter `---` sınırlayıcıları eksik gelmişti,
eklendi. README.md üç yeni görev için pin, komut, tahmini süre, zorluk gerekçesi ve
gerçek referans doğrulama sonuçlarıyla genişletildi.

Üç `verify` adımı da yeşil. `owns` dışına dosya yazılmadı, git'e dokunulmadı (commit
T0'a bırakıldı — B4 çalışırken T0 ara bir commit de attı, üstüne çalışıldı).
